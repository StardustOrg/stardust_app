import 'package:flutter/material.dart';

/// [StarColors] is a class that holds all the colors used in the Stardust app.
class StarColors {
  StarColors._();

  // Primary Colors
  static const Color starPink = Color(0xFFF24E83);
  static const Color starTeal = Color(0xFF1AC4C2);
  static const Color starBlue = Color(0xFF315F9B);

  // Gradient Colors

  // Text Colors

  // Background Colors
  static const Color bgLight = Color(0xFFFDFEF0);

  // Button Colors

  // Border Colors

  // Border Colors

  // Error and Validation Colors

  // Neutral Shades
  static const Color white = Color(0xFFEEEFF1);
  static const Color black = Color(0xFF051D25);
}
